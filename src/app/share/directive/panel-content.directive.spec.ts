import {PanelContentDirective} from './panel-content.directive';

describe('PanelContentDirective', () => {
    it('should create an instance', () => {
        // const directive = new PanelContentDirective();
        // expect(directive).toBeTruthy();
    });
});
